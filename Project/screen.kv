<MenuScreen>:
    BoxLayout:
        Label:
            text: 'hehe, map goes null'
    AnchorLayout:
        anchor_x: 'left'
        anchor_y: 'top'
        padding: [30,30,0,0]
        Button:
            text: 'Goto settings'
            size: 250,75
            size_hint: None, None
            on_press:
                root.manager.transition.direction = 'right'
                root.manager.current = 'settings'

<SettingsScreen>:
    BoxLayout:
        orientation: 'horizontal'
        Label:
            text: 'Settings'
        BoxLayout:
            orientation: 'vertical'
            Button:
                text: 'Back to menu ->'
                on_press:
                    root.manager.transition.direction = 'left'
                    root.manager.current = 'menu'
            Button:
                text: 'My settings button'
            Button:
                text: 'My settings button'
            Button:
                text: 'My settings button'
            Button:
                text: 'My settings button'
            Button:
                text: 'My settings button'
            Button:
                text: 'Shutdown'
                on_press:
                    text:"Exit"
                    on_press: app.stop()
